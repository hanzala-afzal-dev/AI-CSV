# Superseded Specification History

The original `constitution.md`, `000-foundation/`, `001-csv-upload/` and
`002-dataset-profiling/` documents were replaced by the consolidated 000-020 specification set.

They remain available in Git history and are not active contracts. The current implementation mapping is
maintained in `specs/README.md`; architecture decisions remain in `docs/adr/`.
